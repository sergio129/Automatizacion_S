package co.konecta.sura.certificate.utils;

public class SaraPage {

  /*  public SaraPage(WebDriver driver) {
        super(driver);
    }

    protected void campoExistente(By campo, String informacion, Boolean esUnCalendario){
        WebElement elemento = findElement(campo);
        if ( !elemento.getAttribute("value").isEmpty() ){
            elemento.clear();
        }
        elemento.sendKeys(informacion);
        if ( esUnCalendario ){
            quitarFocoDelElemento();
        }
    }*/


}
